resource_group_name = "rg-subnets-example"
location            = "uksouth"

# vnets/subnets are defined in network.json (see network.json.example),
# not here. Set network_definitions_file below to point at your file if
# it's not named network.json.
# network_definitions_file = "network.json"

tags = {
  environment = "dev"
  project     = "terraform-subnets"
}
